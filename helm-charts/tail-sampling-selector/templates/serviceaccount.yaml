{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tail-sampling-selector.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tail-sampling-selector.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
